# SPOTR — TODOS

Generated by /plan-ceo-review on 2026-05-03. Maintained going forward.

## P1 — Pre-Launch Blockers

### [FIXED IN REVIEW] CORS wildcard
- **What:** Restrict `app.use(cors())` to known origins
- **Why:** Any site can make authenticated API calls on behalf of logged-in users
- **Where:** `backend/src/server.ts:26`
- **Effort:** S → S with CC

### [FIXED IN REVIEW] Deal completion auth gap
- **What:** Add party verification to `PUT /api/deals/:id/complete`
- **Why:** Any authenticated user can mark any NIL deal as completed
- **Where:** `backend/src/routes/deals.ts:88`
- **Effort:** S → S with CC

### [FIXED IN REVIEW] DB indexes for feed performance
- **What:** Add `@@index([authorId])` and `@@index([isStory, isReel, createdAt])` to Post model
- **Why:** PostgreSQL does not auto-index foreign keys; feed query scans full table without these
- **Where:** `backend/prisma/schema.prisma`
- **Effort:** S → S with CC

### [FIXED IN REVIEW] Feed pagination
- **What:** Add `limit`/`offset` params to `GET /api/posts/feed`
- **Why:** Hardcoded `take: 50` with no scroll-to-load — early power users hit the bottom
- **Where:** `backend/src/routes/posts.ts:212` + `frontend/app/feed/page.tsx`
- **Effort:** S → S with CC

### [FIXED IN REVIEW] Rate limiting on auth + comments
- **What:** Add `express-rate-limit` to `POST /auth/login` (10/min/IP) and `POST /comments` (30/min/user)
- **Why:** Brute-force login and comment spam both unprotected
- **Where:** `backend/src/server.ts` or per-route middleware
- **Effort:** S → S with CC

### Push notifications
- **What:** Notify athletes/fans when someone likes, comments, or follows
- **Why:** Engagement loop is silent — no reason to open the app unprompted
- **Approach options:** Web Push API (service worker) or OneSignal SDK — both work with Railway
- **Effort:** M (human ~3 days) → S with CC
- **Depends on:** nothing — can ship independently

### Google Sign-In / OAuth
- **What:** Add Google OAuth alongside email/password login
- **Why:** Reduces friction at signup — athletes are on mobile, typing email+password is high drop-off
- **Approach:** Passport.js `passport-google-oauth20` strategy, or migrate to Supabase Auth with Google provider
- **Note:** If migrating to Supabase, this becomes a larger change (see P2 below)
- **Effort:** M (human ~1 day) → S with CC
- **Depends on:** nothing if staying on Railway+Prisma; large if migrating to Supabase

---

## P2 — Post-Launch Improvements

### Story row cleanup
- **What:** Delete expired story rows daily (`isStory=true AND storyExpiresAt < NOW()`)
- **Why:** Expired stories accumulate in posts table indefinitely; feed filters them at query time but rows pile up
- **Approach:** Railway cron job or a `prisma.$executeRaw` cleanup endpoint triggered by a scheduled task
- **Where:** New `backend/src/jobs/cleanupExpiredStories.ts`
- **Effort:** S → S with CC

### Remove redundant likes include from feed/reels
- **What:** Remove `include: { likes: { include: { user: ... } } }` from feed and reels `findMany`
- **Why:** All likes loaded per post per feed load; `_count.likes` + `userLikedPostIds` already cover both needs
- **Where:** `backend/src/routes/posts.ts:152` (feed) and line ~25 (reels)
- **Effort:** S → S with CC

### Add limit to GET /posts/:id/comments
- **What:** Add `take: 50` (or pagination) to `GET /:postId/comments`
- **Why:** Loads all comments for a post with no cap — 200 comments = 200 user joins
- **Where:** `backend/src/routes/comments.ts:55`
- **Effort:** S → S with CC

### parseInt NaN guard on reels pagination
- **What:** Add fallback: `take: parseInt(limit as string) || 20`
- **Why:** `?limit=abc` passes NaN to Prisma, causing a 500 error
- **Where:** `backend/src/routes/posts.ts:88-89`
- **Effort:** S → S with CC

### JWT refresh tokens
- **What:** Add short-lived access tokens (1h) + long-lived refresh tokens (30d)
- **Why:** Current 7-day JWT cannot be revoked — stolen token works for 7 days
- **Effort:** M → S with CC
- **Note:** This becomes simpler if migrating to Supabase Auth

### Prisma db push → migrate
- **What:** Switch from `prisma db push` to `prisma migrate` with named migrations
- **Why:** Once production has real data, `db push` can silently destroy data on conflicting changes
- **Effort:** S for the switch, migration history must be initialized

### Structured logging
- **What:** Replace `console.error` with a structured logger (pino or winston)
- **Why:** No request IDs, no log aggregation, no way to trace a user-reported bug in prod
- **Effort:** M → S with CC

### Tests for critical paths
- **What:** Integration tests for: `PUT /deals/:id/complete`, `PUT /offers/:id/accept`, `PUT /offers/:id/decline`, feed query, story expiry filter
- **Why:** These paths touch financial data (NIL deals) and have zero automated coverage
- **Effort:** M → S with CC

---

## P3 — Feature Backlog

### AI Highlight Mixtape Creation
- **What:** AI-powered highlight reel creator — athlete selects clips, AI cuts a hype mixtape, athlete posts to SPOTR
- **Why:** Turns SPOTR from a social platform into a creation tool — athletes come to create, not just share
- **Vision:** Select clips from uploaded videos → choose music/style → AI generates cut → preview → post to feed as a reel
- **Infra:** Mux is already set up for video. AI video editing likely needs FFmpeg + a model (Runway, Pika, or open-source) or an API (Shotstack, Mux video AI if it ships)
- **Effort:** L (human ~2 weeks) → M with CC (~3-4 days including AI integration)
- **Depends on:** Video upload working reliably (already shipping). AI provider selection TBD.

### Feed algorithm / personalization
- **What:** Replace "show latest 50" with a ranked feed (following + engagement signals)
- **Why:** Current discovery fallback shows everyone — no personalization, no reason to follow
- **Effort:** M → S with CC

### Athlete verification badges
- **What:** Verified badge for real athletes vs. fan accounts
- **Why:** Coaches need to trust they're recruiting real athletes
- **Effort:** S → S with CC

### Supabase migration (optional)
- **What:** Migrate from Railway PostgreSQL + custom auth to Supabase (auth + DB + storage)
- **Why:** Supabase gives you Google OAuth, magic links, row-level security, and a dashboard for free
- **Tradeoff:** Significant migration effort; current Railway stack is working fine. Only worth it if Google Auth + RLS are both wanted
- **Effort:** XL (human ~1 week) → L with CC
- **Note:** If Google Auth is the main ask, use Passport.js on the existing stack — much cheaper
